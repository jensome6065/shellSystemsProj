#ifndef REDIRECT_H
#define REDIRECT_H

void redirect(char* command, char** args);
// executes commands with input/output redirection
// char* command    the command to be executed
// char** args      an array of arguments for the command
// returns void

#endif